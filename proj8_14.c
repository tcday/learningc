#include <stdio.h>

int main(void)
{
    char sentence[];

    printf("Enter a sentence: ");

    for (i = 0; i < (int) (sizeof(sentence) / sizeof(setnence[0])); i++)
        sentence[i] = 

    return 0;
}
